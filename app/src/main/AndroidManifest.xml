<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.superbostudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppStartupTheme">
        <activity android:name=".activity.StartupActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.MainActivity"/>

        <activity android:name=".activity.LoginActivity"
            android:theme="@style/AppDayTheme"/>

        <activity android:name=".activity.DayOneActivity"
            android:theme="@style/AppDayTheme"/>

        <activity android:name=".activity.DayTwoActivity"
            android:theme="@style/AppDayTheme"/>

        <activity android:name=".activity.DayThreeActivity"
            android:theme="@style/AppDayTheme"/>



    </application>

</manifest>